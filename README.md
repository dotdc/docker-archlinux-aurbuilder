# docker-archlinux-aurbuilder

## Description

This repository is used to build an Archlinux Docker image containing all the necessary tools needed to build AUR packages.

## Usage

```console
docker pull imrtfm/archlinux-aurbuilder:latest
```